<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home Page</title>
</head>
<body>
  <%- include('partial/_nav.ejs') %>
  <% if (user) { %>
    <h1>Welcome back to the Babylon Books, <%= user.username %> we missed you!</h1>

      <a href="/users/<%= user._id %>/libraries">Browse Libraries</a> ,
      <a href="/users/<%= user._id %>/books/new">Add Book</a>, or 
      Continue where you left off: <a href="/books/<%= user.currentBookId %>">Last Active Book</a>
    </p>
  
  <% } else { %>
    <h1>Welcome to Babylon Books, guest!</h1>
    <h3>
      We see this is your first time here,
      <a href="/auth/sign-up">Sign up</a> or
      <a href="/auth/sign-in">Sign in</a>.
      to access your library and personalized features!
    </h3>
  <% } %>

</body>
</html>
